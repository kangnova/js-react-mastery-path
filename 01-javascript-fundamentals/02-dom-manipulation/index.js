/** 
 * 🚀 Latihan Tahap 1: DOM Manipulation
 * 
 * Petunjuk:
 * 1. Buka file `step-by-step.md` untuk panduan pengerjaan.
 * 2. Tuliskan kode Anda di bawah ini sesuai langkah-langkah di file tersebut.
 * 3. Simpan perubahan (Ctrl + S) lalu buka `index.html` dan klik tombol "Klik Saya!".
 */

// Tuliskan Kode Anda mulai dari sini:
console.log("File index.js (DOM) berhasil dimuat!");

// Langkah 2: Tangkap Elemen (mainBody, mainTitle, magicBtn)
// Langkah 3: Tambahkan event listener (klik tombol)
// Langkah 4: Ubah teks judul dan warna background di dalam fungsi klik
